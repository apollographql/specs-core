schema
  @core(using: "https://specs.example.com/A/1.0")               # name is A
  @core(using: "https://specs.example.com/A/2.0", as: "A2")     # name is A2
{
  query: Query
}

schema
  @core(using: "https://specs.example.com/A/1.0") # name is A
  @core(using: "https://specs.example.com/A/2.0") # name is A
{
  query: Query
}

schema
  @core(using: "https://specs.example.com/A/1.0")              # name is A
  @core(using: "https://www.specs.com/specA/1.1", as: "A")     # name is A
{
  query: Query
}